framework:
    secret:          secret
    test: ~
    router:          { resource: "%kernel.root_dir%/routing.yml" }
    form:            true
    csrf_protection: false
    validation:      { enable_annotations: true }
    templating:      { engines: ['twig'] }

misteio_cloudinary:
  cloud_name: phraseart
